import { MapDirective } from './map.component';

describe('MapDirective', () => {
  it('should create an instance', () => {
    const directive = new MapDirective();
    expect(directive).toBeTruthy();
  });
});
